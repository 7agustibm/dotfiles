[user]
	signingkey = 2870EA11BFF2A80FE549BC4E8763E54A152569AD
	name = Agusti Becerra Mila
	email = contact@agustibm.com

[github]
    user = 7agustibm

[core]
  editor = vi
  excludesfile = ~/.gitignore
  autocrlf = input

[alias]
  c = commit
  ca = commit -a
  cm = commit -m
  cam = commit -am
  d = diff
  l = log --graph --pretty=format:"%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset" --abbrev-commit
  st = status
  ci = commit
  br = branch
  co = checkout
  dc = diff --cached
  lg = log -p
  lol = log --graph --decorate --pretty=oneline --abbrev-commit
  lola = log --graph --decorate --pretty=oneline --abbrev-commit --all
  ls = ls-files
  merge-to = "!f() { git checkout $1 && git merge $2 && git checkout -; }; f"}
  ign = ls-files -o -i --exclude-standard

[color]
    ui = auto

[color "branch"]
  current = yellow reverse
  local = yellow
  remote = green

[color "diff"]
  meta = yellow bold
  frag = magenta bold
  old = red bold
  new = green bold

[color "status"]
  added = yellow
  changed = green
  untracked = cyan
